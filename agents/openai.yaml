interface:
  display_name: "Verified Completion Loop"
  short_description: "Finish every authorized requirement before closeout"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#22C55E"
  default_prompt: "Use $verified-completion-loop to close out this long task, complete every authorized gap, and verify the final result against the original request."
policy:
  allow_implicit_invocation: true
